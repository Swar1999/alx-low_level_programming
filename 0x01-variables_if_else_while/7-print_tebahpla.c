#include <stdio.h>
/**
 * main - Entry point
 * Description: programe that print alphabet in lowercast
 * Return: Always 0 (Success)
 */
int main(void)
{
	char c;

	c = 'z';
	while
		(c >= 'a') {
			putchar(c);
			c--;
		}
	putchar('\n');
	return (0);
}
