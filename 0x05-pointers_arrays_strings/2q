#include "main.h"

/**
 * swap_int - Entry point
 * Description: program that swap two ints
 * @a: pointer 1
 * @b :pointer 2
 * Return: Always 0 (Success)
 */
void swap_int(int *a, int *b)
{
	int t;
	t = *a;
	*a = *b;
	*b = t;
}
