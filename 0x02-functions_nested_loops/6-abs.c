#include "main.h"
#include <stdio.h>
/**
 * _abs - Entry point
 * Description: program that calculate the absalut value
 * @int : integer
 * Return: Always 0 (Success)
 */
int _abs(int)
{
	int i;
		if (i < 0)
			_putchar(i * -1);
		_putchar(i);
}
