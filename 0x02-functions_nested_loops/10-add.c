#include "main.h"

/**
 * add - Entry point
 * Description: program that add two numbers
 * @n: paramter1
 * @m: paramter2
 * Return: Always 0 (Success)
 */
int add(int n, int m)
{
	int sum = n + m;
		return (sum);
}
